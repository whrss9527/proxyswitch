# 更新日志

## 未发布

### 新增

- 机场订阅：添加配置时填订阅地址，由内置管理的 mihomo（Clash.Meta）内核连接订阅里的节点，不需要再安装 Clash 之类的代理软件。
  - 两种模式：大陆直连（国内的网站和 IP 直接连接，其余走节点）和全部走代理。
  - 在设置窗口的节点列表或托盘菜单的子菜单里选节点、全部测速；「自动选择」使用延迟最低的节点。选中的节点保存在配置文件里。
  - 编辑时可以先「检查订阅」：显示节点数、流量和到期时间，还没填名字时用机场给的名字。
  - 每天自动更新订阅，失败时一小时后重试并继续使用上次的节点；配置列表显示已用流量和到期时间，快用完或快到期时标出来。
  - 支持 Clash 配置和 base64 编码的节点链接两种订阅格式。
  - 内核只在需要时从 mihomo 的官方发布下载固定版本，按发布时写入程序的 SHA-256 校验；也可以指定本机已有的 mihomo 程序。内核在 `127.0.0.1:17890` 提供代理，端口可以修改。
  - 下载内核、订阅和地理数据时会尝试直接连接、正在使用的代理和其他软件设置的系统代理，从 Clash 等软件换过来时可以经它下载。
  - 退出 ProxySwitch 时关闭订阅配置的代理（内核随它退出，系统代理不能指向没有程序在听的端口），「启动时」为保持上次的状态时下次启动重新开启；内核意外退出时自动重启。
- 托盘菜单增加「检查更新」：打开「关于」页并立即检查，有新版本时一键更新；已经发现新版本时显示为「更新到 x.y.z」。

### 修复

- 检查更新时 GitHub 接口超过了访问次数限制（未登录时每个 IP 每小时 60 次，公司网络、代理服务器的出口 IP 很多人共用）会失败并显示「GitHub 返回 HTTP 403」。现在改从发布页查找新版本，同样可以一键更新。

## 2.0.0（2026-09-24）

### 新增

- 设置界面重新设计：独立窗口，参照 Windows 11 的设计，跟随系统的深浅色和强调色；修改立即保存并生效，不再需要「保存并生效」。
- 首次运行引导：自动打开设置窗口，可以自动检测、手动填写或使用 PAC 脚本添加第一个配置。
- 自动检测本机代理：找出正在运行的代理软件监听的端口，确认能转发连接后一键添加。
- 系统代理由其他程序或 Windows 设置开启时，可以一键保存为配置。
- 测速：经代理实际访问测速地址并显示延迟，可以单个测也可以全部测。PAC 配置按脚本为测速地址选出的代理测。
- 复制终端代理命令：托盘菜单和设置窗口可以复制 PowerShell、命令提示符、Git Bash 下设置代理环境变量的命令，已经打开的终端也能用上代理。
- 按网络自动切换：按 Wi-Fi 名称、DNS 后缀或网关匹配规则，网络变化时自动切换配置或关闭代理。
- 健康检查：代理服务器连不上时提醒，或自动关闭代理并在恢复后重新开启。
- 关闭代理时可以恢复开启前的系统代理设置。
- 启动时自动开启或关闭代理。
- 按数字切换配置的快捷键，例如 `Ctrl+Alt+1`。
- 托盘图标改为开关造型，按配置的颜色显示状态，连不上代理服务器时滑块变红；右键菜单跟随系统深色模式，每个配置显示颜色和快捷键。
- 通知可以设为只显示问题或不显示，信息类通知使用程序自己的图标。
- 单击、双击托盘图标的动作可以修改。
- 命令行在托盘程序运行时交给它执行，托盘图标立即更新；再次打开 exe 会打开设置窗口。
- 手动修改配置文件后几秒内自动生效，有错误时提示并继续使用修改前的配置。
- 诊断页：查看系统代理、环境变量、git、npm 的当前设置和运行日志，一键清除所有代理设置，打开 Windows 的代理设置页。
- 程序内更新：关于页一键下载新版本，用发布附带的 SHA256SUMS.txt 校验后替换并重新启动；每天自动检查一次，发现新版本时托盘提示，可以关闭。
- 点击托盘通知打开相关的设置页。
- 程序崩溃时记录详细信息，下次启动时提示，诊断页可以查看。
- 支持 Windows 高对比度主题。
- 导入和导出设置。
- 提供 ARM64 版本。

### 改进

- 系统代理改用 WinINET 的按连接设置接口，同时设置拨号和 VPN 连接，写入后读回确认；被组策略锁定时给出提示。
- 开启代理期间暂时关闭「自动检测设置」，避免网络里的自动配置盖过手动设置，关闭代理时恢复。
- 修改正在使用的配置后立即重新应用；切换配置时清除上一个配置设置过、新配置没有的项。
- 第一次运行不再生成示例配置。
- 日志改为结构化格式，超过 1MB 自动轮转。

### 兼容性

- 1.x 的配置文件可以直接使用，原来的 `notify` 会自动换算成 `notify_level`。

## 1.1.1（2026-09-18）

- 通知默认 3 秒后自动关闭，时长可以设置。

## 1.1.0（2026-09-18）

- 新增图形化设置页面。

## 1.0.0（2026-09-18）

- 第一个版本：托盘开关系统代理，多套配置，PAC，全局快捷键，开机自启，可选设置环境变量、git、npm。
